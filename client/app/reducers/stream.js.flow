// @flow
import type {
    Map,
} from 'immutable';

declare class Remote {
    connection: ?Object,
    stream: ?MediaStream,
}

declare class StreamState {
    joined: boolean,
    hasMicro: boolean,
    hasCamera: boolean,
    localStream: ?MediaStream,
    remotes: Map<string, RemoteRecordType>,
}
